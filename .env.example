# Cambia el nombre de este archivo a ".env" y completa con tu propia API key:
#
# Conseguis tu API key gratis en https://openrouter.ai (Settings -> Keys).
# El archivo .env NO se sube a GitHub (esta en .gitignore).

OPENROUTER_API_KEY=tu-api-key-aca
